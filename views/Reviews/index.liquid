{% layout 'layout.liquid'%}
{% block content %}
<div>
    <h2 class="display-6">All Reviews</h2>
    <div class="row">
        {% for console in consoles %}
        <div class="card" style="width: 18rem;">
            <h5 class="card-header">{{ review.title }}</h5>
            <div class="card-body">
                <h5 class="card-title">
                    Developer: {{ review.content }}
                </h5>
                <p class="card-text">
                    Released: {{ review.console }}
                </p>
                <p class="card-text">
                    Ratings: {{ review.arthor }}
                </p>
                <a href="/consoles/{{ review.id }}" class="btn btn-info">
                    View Reviews{{ review.title }}
                </a>
            </div>
        </div>
        {% endfor %}
    </div>
</div>
{% endblock %}