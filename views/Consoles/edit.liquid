{% layout 'layout.liquid' %}
{% block content %}
<div class="container-sm">
    <form action="/consoles?_method=PUT" method="POST">
        <fieldset>
            <legend>Create New Console</legend>
            <label class="form-label">Title: </label>
            <input 
                type="text" 
                name="title" 
                placeholder="{{ console.title }}"
                value="{{ console.title }}"
                class="form-control"
            >
            <label class="form-label">Body:</label>
            <input 
                type="text" 
                name="body" 
                placeholder="{{ console.body }}" 
                value="{{ console.body }}" 
                class="form-control"
            >
            <label class="form-label">Amount:</label>
            <input 
                type="number" 
                name="amount"
                value="{{ console.amount }}"
                class="form-control"
            >
            <label class="form-label">Ready:</label>
            <input 
                type="checkbox" 
                name="ready" 
                class="form-check-input"
                {% if console.ready %}
                    checked
                {% endif %}
            >
        </fieldset>
        <input type="submit" value="Edit Console" class="btn btn-success">
    </form>
</div>
{% endblock %}